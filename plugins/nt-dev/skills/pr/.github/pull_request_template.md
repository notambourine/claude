<!-- Remove these comments when filling the template. Keep the complete body within 150 words: Goal 40, Summary 60, Key Decisions 30, Test Plan 20. Screenshots do not count. Write one physical line per paragraph, bullet, and checkbox. Leave `_none_` under an empty section, except remove Screenshots for non-visual changes. -->

## Goal

<!-- State who gains what capability and why it matters. Link the issue inline with `Closes #123`, or use `#123` without closing it. A small chore can state only its outcome. -->

## Summary

<!-- Use one bold-lead bullet per affected area. Describe behavior, not files. Include at most one Mermaid diagram and only when a sentence cannot explain the flow or call order. -->

## Key Decisions

<!-- Include at most three choices that could reasonably change the diff: a rejected route, an external constraint, or an intentionally open gap and owner. Use `_none_` when there are none. -->

## Screenshots

<!-- UI changes only. Provide a named before/after pair for each surface, or a recording for motion and multi-step flows. Upload captures as GitHub user attachments; never commit them. -->

<!-- drag screenshots here -->

## Test Plan

<!-- Record manual evidence CI cannot provide. Use checked boxes for completed checks. Use an unchecked box plus its reason for a skipped check. Use `_none_` when there is no manual evidence. -->

- [ ]
